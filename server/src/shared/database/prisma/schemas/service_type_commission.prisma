model ServiceTypeCommission {
  id         String       @id @default(auto()) @map("_id") @db.ObjectId
  name       String       @unique
  createdAt  DateTime     @default(now())
  updatedAt  DateTime     @updatedAt
  Commission Commission[]

  @@map("service_type_commissions")
}
